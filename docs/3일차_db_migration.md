# 3일차: DB 모델 및 마이그레이션 실습

## 1. SQLAlchemy 모델 작성 (`app/models/user.py`)
- ERD를 참고하여 작성한 사용자(User) 테이블 및 ORM 모델 설명

## 2. Alembic 마이그레이션 수행
- Alembic 초기화 및 명령어 실행 과정 (`revision`, `upgrade head`)

## 3. DB 스키마 적용 확인
- MySQL DB Viewer에서 `user` 테이블이 정상 생성된 스키마 화면 캡처 이미지 첨부